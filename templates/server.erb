<% scope['zookeeper::ensemble'].each_with_index do |node_address, index| -%>
server.<%= index.to_i.next %>=<%= node_address %>:<%= scope['zookeeper::peer_port'] %>:<%= scope['zookeeper::leader_port'] %>
<% end -%>
